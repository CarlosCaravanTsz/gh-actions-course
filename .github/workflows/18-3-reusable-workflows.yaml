name: 18 - 3 - Reusable Workflows
on:
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/18-1-reusable-workflows.yaml
    with:
      target-directory: ./my-app
  e2e-tests:
    uses: CarlosCaravanTsz/github-actions-course-example-e2e/.github/workflows/e2e.yaml@master  # <owner>/<repo>/.github/workflows/<workflow_file>@<version>
    secrets: ${{ secrets.GH_TOKEN || ''}}